# 2026-04-24（周五）工作复盘

> 阶段8 收尾：SL 状态补全 + HTML Dashboard 上线

---

## 📌 今日主线

**晚上回家后**验证 launchd 自动跑的结果 → 发现 SL_MISSING 遗留 → 修复 → **搭建 HTML Dashboard 网页版**。完成了从"只能看企业微信消息"到"浏览器里全景可视化"的升级。

---

## ✅ 已完成

### 1. 验证 launchd 21:30 自动跑结果（全绿）

- ✅ launchd LastExitStatus=0
- ✅ 状态恢复：持仓 5 只 + $400,916 现金
- ✅ per_symbol 映射生效：scan 28 只产 6 BUY + 2 SELL
- ✅ 风控拦截：5 只已持仓全部 "Already holds" REJECTED，CAT 触发 "Max positions 5/5"
- ✅ 执行 0 笔订单（正确决策"维持现状"）
- ✅ 对账报告生成 + 企业微信推送

### 2. 修复 SL_MISSING 遗留问题

**问题**：MSFT/NVDA 是阶段7 旧代码买入的，当时 ATR 没传入 `track_position` → 没有止损保护

**修复**：写临时修复脚本，从 DB 拿历史成本 + 最新 ATR，重新注册到 StopLossManager

**结果**：5 只持仓都有完整 ATR 追踪
| 标的 | 入场 | 止损 | TP1 | TP2 | TP3 |
|------|------|------|-----|-----|-----|
| MSFT | $425.24 | $405.79 | $434.97 | $444.69 | $459.28 |
| NVDA | $201.24 | $191.50 | $206.11 | $210.98 | $218.28 |
| META | $672.40 | $633.78 | $691.71 | $711.02 | $739.99 |
| TSM | $386.89 | $363.44 | $398.61 | $410.33 | $427.92 |
| AVGO | $426.19 | $401.54 | $438.51 | $450.83 | $469.32 |

### 3. 🎨 HTML Dashboard 网页版上线

**核心交付**：`scripts/build_dashboard.py` + `run_dashboard.sh`（700+ 行）

**6 大视图**：
1. **净值曲线**（Chart.js 折线图，每日总资产）
2. **KPI 概览**（4 卡片：总资产/现金/市值/交易数）
3. **对账异常列表**（SL_MISSING/HALTED 等警告）
4. **持仓权重饼图**（Chart.js doughnut）+ **持仓表格**
5. **ATR 止损追踪**（止损价/TP1/TP2/TP3/保本状态/距止损百分比）
6. **策略表现对比**（Chart.js 柱状图 + 统计表）
7. **交易历史时间线**（最新 50 条，带 markdown 样式）

**设计亮点**：
- ✅ **涨红跌绿**（符合中国股民习惯，CSS 变量 `--color-up: #d32f2f`）
- ✅ **单文件 HTML**（18KB，Chart.js 走 CDN）
- ✅ **响应式布局**（grid-2 / grid-4 自动切换）
- ✅ **零前端依赖**（不需要 npm / 构建）

**使用方式**：
```bash
./run_dashboard.sh              # 生成 + 自动打开浏览器
./run_dashboard.sh --no-open    # 只生成
```

---

## 📊 今日关键数据

### 账户状态（与昨晚一致，今天没有新交易）

| 指标 | 值 |
|------|------|
| 总资产 | $799,094.71 |
| 现金 | $400,916.35 |
| 持仓市值 | $398,178.36 |
| 累计收益率 | -0.11% |
| 持仓 | 5 只（MSFT/NVDA/META/TSM/AVGO） |

### 对账异常

- **修复前**：2 条 WARNING（SL_MISSING）
- **修复后**：0 条异常 ✅

### Dashboard 统计

- 文件大小：18KB
- Canvas/Chart 数量：3 个（净值 + 饼图 + 柱图）
- 表格数量：3 个（持仓 / ATR 止损 / 策略统计）
- 时间线：11 条交易记录

---

## 📝 代码变更清单

### 新增文件
- `scripts/build_dashboard.py`（700+ 行）：HTML Dashboard 生成器
- `run_dashboard.sh`：快捷脚本（自动检测 python + 打开浏览器）

### 修改文件
- `data_cache/quant.db`：MSFT/NVDA 补全 ATR 状态（通过 trading_state）

### 输出产物
- `output/dashboard.html`：主 HTML 文件（18KB）

---

## 🚧 待办

### 🔥 明天必做
- [ ] **观察 launchd 周五 21:30 自动触发**（美股周六休市，周一才出新数据）
- [ ] 打开 Dashboard 在手机/iPad 上试试响应式布局

### 📅 本周末计划
- [ ] **深度分析 watchlist_screen.csv**：找出被同一策略打败的多标的模式
- [ ] **完善 dev-09 多因子规划文档**：加"因子有效性初步验证"章节
- [ ] **考虑：Dashboard 加实时刷新**（每 N 分钟自动重新生成 HTML + WebSocket 推送）

### 🗓 下周及以后
- [ ] **阶段9 多因子选股实施**（2-3 周）
- [ ] **实盘准备**（Paper 跑满 4 周且年化 > 5% 后）
- [ ] **接入财报数据**（PE/ROE/营收增速）

### 💡 待研究问题
- [ ] 🤔 净值曲线现在只有占位数据，**需要从 DB 的 `daily_performance` 表读真实数据**（目前表为空，因为还没跑完整一天）
- [ ] 🤔 Dashboard 是否加"最近 7 天成交热力图"
- [ ] 🤔 交易历史时间线如果有 SELL 止损能在旁边标一下"盈亏金额"

---

## 🧠 今日反思

### 做得好的
- **先修风控再做 Dashboard**：选 A 路径是对的。SL_MISSING 如果没修，Dashboard 里也会红色警告一片，视觉上很糟
- **从 trading_state 重建 SL**：用现成的机制补全数据，没造新轮子
- **HTML 单文件方案**：Chart.js CDN + inline CSS，零前端依赖，简单可靠

### 可以更好的
- **daily_performance 表一直是空的**：`PaperTrader.take_daily_snapshot` 只在 `DailyReconciliation.run()` 里调用了一次，但表中没数据？需要排查。目前净值曲线只能用占位值，没有历史真实数据
- **Dashboard 没有"回测数据对比"**：应该能看到"Paper 真实表现 vs 回测预期"的差距
- **修 SL_MISSING 用的是一次性脚本**：应该沉淀到 `PaperTrader.load_state` 里，检测到持仓存在但 SL 不存在时自动重建

### 学到的
1. **数据持久化"漏水"是累积的**：阶段7 的小遗漏在阶段8 才暴露，说明**每次改动都要检查"所有相关状态是否一致"**
2. **可视化的价值在"暴露问题"**：Dashboard 上线后 SL_MISSING 的严重性一目了然，之前看企业微信消息没这种震撼
3. **Chart.js 的 CDN 方案够用了**：不需要引入 npm 和构建，项目规模还小的时候简单就是美

---

## 🏆 战果总结

- **代码**：~750 行新增（700 行 build_dashboard + 50 行 shell/修复）
- **新脚本**：2 个（build_dashboard.py + run_dashboard.sh）
- **异常修复**：SL_MISSING 清零
- **视图新增**：6 大视图全部上线
- **总工时**：~2.5 小时（22:30-01:00）

---

_Last updated: 2026-04-25 00:10_

---

## 🌌 23:22 - Dashboard Cyberpunk 升级（Round 2）

### 用户痛点
1. Dashboard 不会自动更新，每次都要手动跑 `./run_dashboard.sh`
2. 视觉太朴素（白底黑字），缺乏科技感

### 解决方案

**1. 自动触发**（5 行代码）
- `scripts/run_paper_trade.py` 在 `--daily-scan` 完成后用 subprocess 调 `build_dashboard.py`
- 失败不影响主流程（只 log warning）
- 修了一个 bug：`project_root` 局部变量错用 → 改为 `orch.project_root`

**2. Cyberpunk 暗黑科技风**（HTML 模板大改）

视觉升级：
- 配色：深蓝紫渐变背景（#050817 → #0a0e27）+ 霓虹青色（#00f0ff）+ 霓虹紫（#b388ff）+ 霓虹粉（#ff4dd2）
- 涨红跌绿改用霓虹版（#ff3b5c / #00ff9c）+ 文字发光效果
- 字体：Orbitron（标题，未来感）+ Space Grotesk（正文）+ JetBrains Mono（数字）
- 卡片：毛玻璃 backdrop-filter blur 24px + 悬停浮起 + 边框发光
- 动效：Header 顶部 scan-line 动画、live-dot 脉冲、卡片入场 fadeInUp、timeline-marker 脉冲扩散
- 网格底纹：fixed 全屏 48px 网格点阵
- 滚动条：霓虹青色带发光

图表升级：Chart.js → ApexCharts 3.49
- 净值曲线：area chart + 渐变填充 + smooth curve（涨红/跌绿自适应）
- 持仓饼图：donut + 中心显示 TOTAL MV + 数据标签百分比
- 策略柱状图：圆角柱 + 顶部 dataLabel + 涨红跌绿对比

### 文件变更
- `scripts/build_dashboard.py` HTML_TEMPLATE 整体改写（CSS ~280 行 + JS 重写 ~180 行）
- `scripts/run_paper_trade.py` daily-scan 末尾自动调 build_dashboard

### 验证
- ✅ 单独跑 build_dashboard：HTML 18KB → 31KB
- ✅ 跑 daily-scan：自动触发"📊 自动生成 Dashboard... ✅ Dashboard 已更新"
- ✅ 浏览器打开效果完全符合 Cyberpunk 设计稿

### 学到的
- Python f-string 模板里写 JS 要用 `{{ }}` 转义 `{ }`，但容易漏掉
- subprocess.run 调子脚本时 `project_root` 必须用 `orch.project_root` 而不是 `__init__` 局部变量

---

## 🎉 23:42 - Git 仓库正式上线 GitHub

### 背景
项目 5700+ 行代码居然没有版本控制，强烈隐患——电脑挂了/手抖删了全没。今晚必须解决。

### 操作流程
1. **加固 .gitignore**：追加 `.codebuddy/` `.workbuddy/` `/.obsidian/`、Obsidian workspace 缓存、pytest cache 等
2. **`git init -b main`**：本地仓库初始化
3. **三轮脱敏验证**：
   - status 看 ignored 命中
   - check-ignore -v 验证 `.env`/`monitor.local.yaml`/`data_cache/`
   - log -p 全文搜真凭证（误报排查：`.env.example` 的 `your_app_key_here` 是占位符）
4. **First commit**：94 文件 18,711 行，带详细 message（阶段1-8 全量）
5. **SSH 接入**：
   - 现有 `id_rsa` (2021)
   - GitHub host key 已变（2023 事件），用 `ssh-keygen -R github.com` 清掉重连
   - 公钥手动加到 GitHub Settings → SSH Keys
   - `ssh -T git@github.com` 验证 → "Hi gavincjzou! successfully authenticated"
6. **Push**：`git push -u origin main` → `* [new branch] main -> main`
7. **远程验证**：
   - `git ls-tree origin/main` 94 文件全部安全
   - 0 敏感文件命中

### 仓库信息
- URL：https://github.com/gavincjzou/ai_quant
- Visibility：Private 🔒
- 分支：main（默认 + 跟踪 origin/main）
- Auth：SSH（id_rsa）

### Git 日常使用约定（已确立）
- 每天工作完成 commit + push 一次（备份）
- Commit message 用约定式：`feat: / fix: / refactor: / docs: / test: / chore:`
- main 分支直接开发（单人项目无需复杂分支策略）
- 大重构/试验才开 branch（`experiment/xxx`）

### 学到的
- macOS `pbcopy` 复制成功不输出任何东西（不是 bug）
- GitHub 2023 年换过 SSH host key，旧 known_hosts 必须用 `ssh-keygen -R` 清掉
- 量化项目 .gitignore 必须包含：`.env` / `*.local.yaml` / `data_cache/` / `output/` / `.venv/`

---

## 🔧 23:47-00:00 - 净值曲线修复 + README 完善

### Part 1：净值曲线修复

**诊断**：daily_performance 表空 → Dashboard 净值曲线只能占位。根因：`take_daily_snapshot` 方法存在但从没被 daily-scan 流程调用。

**修复**：
- `PaperTrader.take_daily_snapshot` 接受 `scan_date` 参数（补跑历史日期用）+ 新增 `cumulative_return/trade_count` 字段
- `run_paper_trade.py` daily-scan 循环里每天补跑后调一次 take_snapshot
- `scripts/backfill_equity_curve.py` 新脚本：用 trade_records + K 线历史收盘价反推每日净值
- `build_dashboard.py` build_equity_curve 优先读 daily_performance 表（而不是从 trades 近似）

**验证**：补回 3 天数据（04-22 $799,586 → 04-23 $799,095 → 04-24 $799,095），Dashboard 展示真实数据。

**Commit**：`0a168db fix: 修复净值曲线 + daily_performance 持久化`

### Part 2：README 完善

从 64 行升级到 **430+ 行**，包含：
1. Badge + 标题居中
2. 目录（9 节）
3. 项目简介（解决什么问题 / 目标用户 / 当前状态）
4. 系统架构（ASCII 图 + 模块职责表）
5. 核心特性（策略 / 风控 / 自动化 / 监控）
6. 快速开始（8 步完整流程）
7. 项目结构（全树）
8. 开发里程碑（阶段 1-9 简表）
9. 开发约定（Commit 规范 / 分支策略 / 安全红线 / 测试策略 / 风控原则）
10. 常用命令（运维 / 开发 / Git）
11. 风险提示

### 学到的

- `take_daily_snapshot` 用 `datetime.now()` 是个坑，补跑历史日期时必须显式传入 `scan_date`
- `daily_performance` 表有 UNIQUE(trade_mode, date) + INSERT OR REPLACE 天然幂等，补跑多次无副作用
- README 就是项目门面，花 1h 写好值回票价
