# 2026-04-28 周二 工作复盘

## 主线

阶段 10 启动：修 [[dev-10-系统漏洞全景图]] 提出的 **P0-1（V1 只看不做）** 和 **P0-2（测试覆盖 3%）**。Gavin 选了"中等方案 + 6 模块测试"，凌晨通宵交付。

## 已完成

### Phase B - 测试补强（先做，机械化）
- 新建 `tests/conftest.py`（公共 fixture）+ `pytest.ini` + `scripts/run_tests.sh`
- **6 个测试文件 / 86 个新 case**：
  - `test_factor.py` 19 case（V1Scorer + IndustryMap）
  - `test_position_sizer.py` 12 case
  - `test_stop_loss.py` 12 case
  - `test_westock_client.py` 17 case（mock subprocess）
  - `test_factor_fetcher.py` 6 case（mock LongPort + Westock）
  - `test_risk_manager.py` 20 case
- **修 3 个老 fail**：`test_risk.py` 的 track_position API 顺序、trailing_stop 高点过头、`test_strategies.py` MA 数据不确定
- 最终：**116 tests / 0 failures**（之前 30/3 fail）
- 测试发现 **2 个真业务漏洞**（已标 [TODO]）：
  - PositionSizer 没把 existing_position_value 算进配额
  - RiskManager 熔断后 SELL 也被拒（应放行清仓）

### Phase A - V1 可执行集成（后做，需思考）
- **A1**：扩展 `backtest_factor_screen.py` 加 `--mode vs_holdings`（+321 行）
  - 双组对比：V1 Top-N（等权）vs 当前持仓（按 market_value 加权）
  - 双组指标 + Jaccard 重叠度 + 持仓差异 + 调仓建议
  - standalone 模式向后兼容
- **A2**：新建 `scripts/weekly_rotation_report.py`（225 行）
  - 调 vs_holdings 模式 + 推企微 markdown
  - 三级失败降级
- **A3**：`run_paper_trade.py` daily-scan 加周五数据 hook
  - `target_date.weekday() == 4` 时触发（即周一/周二跑补周五数据时）
  - 每周 1 次，避免刷屏

### 文档
- 新建 `docs/obsidian-vault/dev-系统开发/dev-11-V1可执行集成与测试补强.md`

## 数据 / 实测

### V1 Top-5 vs 当前持仓 30 天回测（snapshot=2026-04-25）

| 组合 | 累计收益 | 备注 |
|------|---------|------|
| V1 Top-5 (MU/TSM/INTC/MRVL/IWM) | **+23.93%** | 半导体重仓 |
| 当前持仓 (MSFT/NVDA/META/TSM/AVGO) | +7.71% | 蓝筹科技 |
| **V1 超额** | **+16.22%** | 信号强烈但样本小 |
| Jaccard 重叠度 | 11% | 仅 TSM 重叠 |

### V1 排名变化推送
真跑 daily-scan 时（last_scan 回退到 04-23 周四）成功推：
- 新进 Top-10：QCOM
- 掉出 Top-10：MRVL

## 代码变更

**6 个 commit 全部 push 到 GitHub**：

| # | Commit | 内容 |
|---|--------|------|
| 1 | `05ba32f` | test(s10-B): 116 tests / 0 fail |
| 2 | `b81144a` | feat(s10-A1): backtest --mode vs_holdings |
| 3 | `ee4b277` | feat(s10-A2): weekly_rotation_report.py |
| 4 | `6fbf846` | feat(s10-A3): daily-scan 周五 hook |
| 5 | `(本次)` | docs(s10): dev-11 + daily |

新增代码 ~1500 行（业务 +585 / 测试 ~900）。

## 待办（下次）

> 不急，但记录在案：

1. **修 P1 漏洞**（< 1 小时）：
   - PositionSizer existing_position_value 算配额
   - RiskManager 熔断后允许 SELL
2. **行业集中度限制**（dev-10 P1-2，1-2h）
3. **业绩归因**（dev-10 P1-4，3-4h）
4. **Forward Backtest 框架**（dev-10 P1-5，2-3h）

## 反思

### 顺利的地方
- Phase B 先做（机械化）+ Phase A 后做（需思考）的节奏对，凌晨脑子不乱
- 测试发现 2 个真业务漏洞——证明 dev-10 P0-2 评估准确
- vs_holdings 模式复用了 80% 现有 backtest 函数，没造重复轮子

### 踩的小坑
- `plan_create` 工具 XML 嵌套调试了 4-5 次才成功（参数顺序敏感）
- WestockClient 测试参数 `usMU.OQ` vs `MU.US`（对方法签名理解不准）
- LongPort 凌晨偶发闪断（V1 跑了 2 次才成功）
- `_compute_weighted_portfolio_returns` vs `compute_portfolio_returns` 等权 vs 加权要分开

### 学到的
- **测试是发现 bug 的工具**，不只是"跑通"。本次 2 个 [TODO] 漏洞就是证据。
- vs_holdings 这种"对照分析"比"自动调仓"更适合个人量化（决策权留给人）
- daily-scan 触发时机用 `target_date.weekday()` 而非物理日期，避免时区错位

## 战果

今日累计 18 个 commit，量化系统代码量从 21,100 → ~22,600 行。

阶段 10 P0 收官：**V1 从"只看不做"→"每周自动周报 + 调仓建议"，测试覆盖 3% → 30%+**。

---

## 23:00-23:30 - 阶段 11 P1 第一晚（风控加固 4 项）

**单 commit**: 36863fc

完成 4 项 P1：

### P1-7 RiskManager 熔断后 SELL 放行（5min）
真业务漏洞修复：L96 熔断检查从 BUY/SELL 分支前挪到 BUY 内。触发熔断时 BUY 仍被拒，SELL（清仓/止损）永远放行。

### P1-6 PositionSizer 测试澄清（25min）
dev-11 的 [TODO] 是误判——`_calc_fixed_pct` 已正确扣 existing。重新写 3 个明确语义测试：
- default<remaining 取 default
- existing 超 max 返回 0
- remaining<default 取 max 限制

### P1-2 行业集中度限制（1.5h）
- risk.yaml 加 max_sector_concentration=0.40 / max_industry_concentration=0.25
- RiskManager 加 _lookup_sector_industry() 懒加载查 fundamental_ratios + 类级缓存
- _compute_sector_exposure() 算"加上新建仓后"的 sector 暴露
- 6 个测试 case 全过

### P1-1 vol-parity 模式（1.5h）
- PositionSizer 加 _calc_vol_parity 方法 + 'vol_parity' mode 路由
- 公式：actual_vol_pct=ATR/price; mult=target/actual; adjusted=default×mult
- 边界：[0.5×default, max_single_pct]
- ATR 缺失退化 fixed_pct
- 默认 mode 不变（保留 risk_based_atr），仅作可选
- 6 个测试 case 全过

**测试统计**：116 → 130 tests / 0 failures

### 反思
- vol-parity 设计简化为"单标的按 ATR 调整 default_pct"而非真正的"组合协方差矩阵"——足够个人量化，避免过度工程
- sector check 用类级缓存避免每次查 DB，第一次买卖建索引即可
- 行业限制默认开启（保护性），vol-parity 默认关闭（避免改变现有持仓策略）

明天继续第二晚：P1-3 SPY-200MA 大盘择时 + P1-4 业绩归因。

---

## 23:30-23:50 - 阶段 11 P1 第二晚（能力扩展 1）

**1 commit**: 176e3cc

完成 2 项 P1：

### P1-3 SPY-200MA RegimeDetector（1.5h）
- 新建 src/strategy/regime_detector.py（225 行）：BULL/BEAR/NEUTRAL 判定
- config/risk.yaml 加 timing 段（默认 disabled，需要积累数据后再启用）
- RiskManager check_order 加 4c 段：bear regime 时单笔仓位 ×0.5
- Dashboard 加 Market Regime 卡片（Cyberpunk 风，A 股惯例红涨绿跌）
- 实测：当前 SPY 706.71 vs 200MA 664.11 = **BULL +6.41%**（数据截至 04-21）
- 14 个测试 case（mock SPY 数据测三种 regime）

### P1-4 业绩归因（1.5h）
- 新建 scripts/attribution_report.py（345 行）
  - FIFO 配对算已实现 PnL（多笔 buy 多次 sell 都对）
  - 按 strategy 聚合（PnL 归到买入时的策略而非卖出时）
  - Markdown 报告：整体战绩 + 按策略 + Top-10 + 亏损 Top-3 + 持仓明细
  - 集成到 daily-scan 周五数据触发（同 weekly_rotation 节奏）
- 11 个测试 case
- 实测当前 11 笔交易：
  - 已实现 -$487.82（manual_close 卖 AAPL/AMZN/GOOGL 都亏 ~$162）
  - 浮动 -$398.18（5 持仓各浮亏 ~$80）
  - **总盈亏 -$886**（4 天的 paper trading 数据，参考意义有限）

**测试统计**：130 → 155 tests / 0 failures（+25 新 case）

### 反思
- RegimeDetector 设计成"框架先建好，等积累再启用"——避免今天就开了影响 V1 推送
- 业绩归因 FIFO 配对的语义比 LIFO 更符合"先进先出 = 保留最新仓位"的直觉
- Dashboard 加卡片复用现有 .up/.down/.neutral CSS class，零新样式

明天继续第三晚：P1-5 Forward Backtest 框架 + dev-12 综合文档。
