---
date: 2026-04-30
mood: 警觉
weather: 多云
---

# 2026-04-30 复盘

## 主线

本周回顾 paper trading + 输出诊断报告 dev-13。

## 已完成

### 20:00-20:35 - 本周诊断 + dev-13 文档

**1 commit**: dev-13-本周回顾与诊断.md

**做了什么**：
- 用真实数据摸底本周 paper trading 状态
- 实测 LongPort API：5 只持仓全部能拉到 04-29 收盘价（API 正常）
- 定位双根因：
  - **根因 A（P0）**：daily-scan 没刷持仓现价（grep 全文 0 行匹配 save_kline / fetch_kline）
  - **根因 B（P0）**：V1 在 connect_quote → QuoteContext(cfg) 拉 socket token 时偶发 Connect error，不在 _retry 装饰范围
- 输出 dev-13（约 350 行）：8 章 / 双根因深度定位 / P0/P1/P2 修复路线图 / 今晚 vs 周末 vs 长期建议 / 反建议 / 命令清单

**关键发现**：
- 8 天 daily_perf 数据完全相同（market_value 一字不变）
- 真账户实际 +$656（NVDA +4% / TSM +1.9%），系统显示 -$398（开仓滑点）
- V1 本周 0 新增 snapshot（仍只有 04-25 / 04-28）
- 周报 / 归因 / forward backtest 0 次触发（被 V1 失败连锁拖累）

## 数据

| 维度 | 状态 |
|---|---|
| 持仓数量/成本 | ✅ 完整 |
| 真实浮盈亏 | +$656 |
| 系统显示浮盈亏 | -$398 |
| 偏差 | +$1054 |
| V1 本周新增 snapshot | 0 |
| 本周新交易 | 0 |
| LongPort API 实测 | ✅ 100% 可用 |

## 代码变更

**新增文件**（1）：
- docs/obsidian-vault/dev-系统开发/dev-13-本周回顾与诊断.md（约 350 行）

**未动代码**：本次纯诊断，按用户要求"不动代码让我自己挑"。

## 待办

### 今晚（待用户决策）
- [ ] P0-1：daily-scan 加"刷持仓现价"步骤（🟢 小 / 零风险）
- [ ] P1-1：数据陈旧度告警（🟢 小）

### 周末（待用户决策）
- [ ] P0-2：connect_quote 独立重试 + V1 socket token 失败降级 REST（🟡 中）
- [ ] P1-3：手动补跑 --replay-date 指令（🟡 中，补本周 8 天 daily_perf）

### 长期
- P1-2 / P2-1 / P2-2 / P2-3

## 反思

**预估教训再确认**：
- 上次承诺"不报小时数"，这次坚持执行 → 用 🟢 小 / 🟡 中 / 🟠 大 三档代替

**诊断方法论收获**：
- 不要预设"是数据源问题"——直接调 API 拿证据是 30 秒就能做的事
- 不要看到"daily_perf 不变"就以为是 launchd 没跑——多查一层（kline_data 也没变）就能定位到刷价缺失
- grep "save_kline\|fetch_kline" 0 行匹配 → 直接从代码层证伪"它在跑只是没成功"

**业务洞察**：
- 阶段 7 当晚验证不能覆盖"开仓后第二天起"的 bug——下次设计要加"T+N 长期运行"的回归测试
- exception 被吞掉 + 没有"市值连续不变"告警 → 这是 P1-1 必修的根本原因
- launchd 跑成功 ≠ 业务跑成功（健康度评分 P2-2 的设计动机）

## 战果

| 项目 | 数值 |
|---|---|
| 新增文档 | dev-13（约 350 行）|
| 实测命令 | 7 次 LongPort + SQLite + grep |
| 定位根因 | 2 个（P0 级）|
| 修复路线图 | 8 项（P0×2 / P1×3 / P2×3）|
| 不动代码 | ✅ 守住承诺 |

---

## 20:38-20:45 - P0-1 + P0-2 修复（约 7min 实际）

**1 commit**（待 push）

### P0-1：daily-scan 加 _refresh_position_prices
- scripts/run_paper_trade.py：新增 `_refresh_position_prices` 方法（约 65 行）
- 在 daily_scan 处理每个 gap 日 `self.scan()` 之前调用
- 关键复用：trader.update_prices（已存在）+ fetcher.fetch_history（已存在）
- 失败策略：单只失败 warn 继续 / 整体失败不抛异常 / 空持仓直接跳过

### P0-2：connect_quote 独立重试 + V1 失败告警
- src/data/longport_client.py：connect_quote 加 5 次指数退避重试（2/4/8/16 秒）
  - 关键：因为 QuoteContext(cfg) 构造函数内部拉 socket/token 抛异常，不在 _retry 装饰范围
  - 必须在构造层加重试
- scripts/run_paper_trade.py：V1 失败时推企微告警（含 socket/token 识别）
  - 避免再次出现"8 天 V1 全失败但没人发现"的悲剧

### 测试 + 验证
- 新增 tests/test_p0_fixes.py（10 case，全 mock）
  - TestRefreshPositionPrices: 6 case（空 / 全成功 / 部分失败 / 空 df / 异常 close / fetch 抛异常）
  - TestConnectQuoteRetry: 4 case（首次成功 / 重试后成功 / 全失败抛 / 缓存复用）
- **177 tests / 0 fail**（167 → 177）
- 端到端验证：
  - kline_data 5 只持仓全部更新到 04-29 ✅
  - daily_perf 04-29 record：market_value 398178 → 365713（用真实价）/ daily_pnl 0 → +$1,230 ✅
  - V1 跑通生成 factor_screen_2026-04-30_v1.md（无 socket token 失败）✅
  - 持仓 SQLite 持久化真实浮盈亏 -$398 → +$198 ✅

### 真实账户揭示
| 持仓 | 修复前显示 | 修复后真实 |
|---|---|---|
| MSFT | -$80 | -$147 |
| NVDA | -$80 | **+$3,180** ⬆️ |
| META | -$79 | -$387 |
| TSM | -$79 | **+$1,430** ⬆️ |
| AVGO | -$80 | **-$3,878** ⬇️ |
| **合计** | -$398 | **+$198** |

NVDA/TSM 大涨救场，AVGO 大跌没看见——**风控视角下次要加单只跌幅告警**（候选 P1）。

## 反思（追加）

**本次执行节奏**：
- Pre-read 找到 update_prices + fetch_history 已存在 → 改 0 行内部逻辑，只在编排层加 65 行
- 复用率高 → 改完测试一把过
- 端到端验证看到真实数字，比写注释强 100 倍

**新洞察**：
- 之前以为 NVDA/TSM 涨救场 = 真实 +$656，实际算 AVGO 跌后 = +$198，更精确
- AVGO 修复前的 cur=425（成本 290）本身就是 bug 数据（应该是开仓时设的，但不知怎么设错）→ 不影响修复，但需要单独排查


---

## 20:53-21:05 - P1-1 + P1-2 + P1-3 修复（约 12min 实际）

**1 commit**（本次提交，含顺手修的 alerter.send 签名 bug）

### P1-1：数据陈旧度告警
- scripts/run_paper_trade.py 新增 `_check_data_staleness`（约 130 行）
  - 三类检查：daily_perf market_value spread / kline_data 持仓陈旧 / factor_snapshots V1 陈旧
  - 阈值从 risk_cfg.staleness_check 读，默认 perf=3/kline=3/factor=5 天
  - 告警自动推企微，含原因排查指南
- daily_scan return 前自动调用，独立 try/except 不影响主流程

### P1-2：V1 失败但有历史 snapshot 时下游照跑
- 新增 v1_can_run_downstream 变量
- V1 失败时检查 DB 是否有任何 V1 历史 snapshot，有则允许下游周报触发
- 不写假数据（避免污染），让 weekly_rotation_report 自带的 fallback 逻辑用最新历史日期
- 关键设计：FactorNotifier 仍用 v1_ok（推"今天 vs 昨天"排名变化，没新数据没意义）；周报/归因/forward 用 v1_can_run_downstream

### P1-3：--replay-date 补跑机制
- 新增 `replay_dates(from_date, to_date, dry_run)` 方法（约 165 行）
- 一次拉 30 根历史 K 线，逐日按日期查 close → update_prices → take_daily_snapshot
- 缺数 fallback：用最近一个交易日的 close
- save_to_db 跟随 dry_run（dry_run=True 不写库）
- 命令行：--replay-date / --replay-to，dry_run 默认开

### 顺手修的 bug
- 之前 P0-2 的 V1 失败告警 + 本次的陈旧告警都用错 `alerter.send(body=...)` 签名
- 实际签名是 `send(message, level, title, tags, markdown)`
- 没真触发过所以一直没暴露，端到端验证时崩了
- 改成 `alerter.warning(text, title=..., tags=..., markdown=...)` 更简洁
- 教训：写新告警代码时必须先看一眼 alerter 真实签名

### 测试
- 新增 tests/test_p1_fixes.py 共 12 case
  - TestCheckDataStaleness: 4 case（无问题 / perf 卡住 / kline 陈旧 / factor 无数据）
  - TestReplayDates: 7 case（参数校验 / 无持仓 / dry-run / 真跑 / 周末跳过 / 缺数 fallback）
  - TestV1FallbackQuery: 1 case（SQL 字符串守护测试）
- 全套：177 → **189 tests / 0 fail**

### 端到端验证
- ✅ replay 真补本周 4 天历史，daily_perf 表已用真实价覆盖
- ✅ 揭示真账户本周走势：起点 800k → 04-23 跌到 795k → 04-24 涨到 805k → **04-27 最高 807k** → 04-28 回落 800k
- ✅ 陈旧告警真触发到企微（已发澄清消息说明是测试）
- ✅ V1 跑通无 socket/token 错误（P0-2 持续生效）

## 反思（追加）

**本次执行节奏**：
- Pre-read 发现 weekly_rotation_report 已自带 fallback → P1-2 改成"允许下游用历史 snapshot"，比"写假数据"安全 N 倍
- replay 设计上"一次拉长 K 线 + 按日期查表"避免 N 个 API 调用，比逐日拉快很多
- 端到端验证时撞到 alerter.send 签名 bug → 必须真跑才会暴露

**新洞察**：
- daily-scan 末尾的陈旧度检查独立 try/except → 不影响主流程，但告警必须正确
- replay take_daily_snapshot 用 risk_manager._daily_pnl（当前内存值），不是真历史日盈亏 → daily_pnl 列不准但 total_assets/market_value 准
- 04-25 周六 daily_perf 表里有 bug 数据没被覆盖（calendar 跳过了 replay）→ 后续单独清理

**P1 战绩**：
- 测试 116 → 189（+73 case，从阶段 11 起算）
- 7 项 P0 + P1 全修
- 系统从"瞎了 8 天没人知道" → "三层兜底（刷价 + 陈旧告警 + 补跑机制）"

